{
  "name": "weave",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "cd backend && node server.js",
    "build": "cd backend && npm install"
  }
}